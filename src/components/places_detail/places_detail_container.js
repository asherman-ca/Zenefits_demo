import { connect } from 'react-redux';
import PlacesDetail from './places_detail';

const mapStateToProps = () => ({
});

const mapDispatchToProps = () => {

};


export default connect(mapStateToProps, mapDispatchToProps)(PlacesDetail);
